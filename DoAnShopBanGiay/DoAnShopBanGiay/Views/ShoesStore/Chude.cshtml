@model IEnumerable<DoAnShopBanGiay.Models.CHUDE>
<p class="lead"> CHỦ ĐỀ GIÀY</p>
<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="@Url.Action("SPTheochude", "ShoesStore", new { id =item.MaCD})" class = "list-group-item">
            @item.TenChuDe
        </a>
    }
</div>
